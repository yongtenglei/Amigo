name: lint

on:
  workflow_call:
    inputs:
      amigo_repository:
        type: string
        required: false
        description: Repository containing shared CI assets.
      directory:
        type: string
        required: false
        description: Path to the directory to run golangci-lint in.
      golangci_path:
        type: string
        required: false
        description: Path to the golangci-lint config file.
      golangci_version:
        type: string
        required: false
        description: Version of golangci-lint to use.
      go-version:
        type: string
        required: false
        description: Go version override. When empty, go-version-file selects the version.
      go-version-file:
        type: string
        required: false
        default: go.mod
        description: Version file relative to the target repository.
      os:
        type: string
        required: false
        default: '["ubuntu-latest","macos-latest","windows-latest"]'
        description: JSON array of runner labels for the lint matrix.
      timeout:
        type: string
        required: false
        description: Timeout for golangci-lint.
permissions:
  contents: read
  pull-requests: read

jobs:
  lint:
    strategy:
      matrix:
        os: ${{ fromJSON(inputs.os) }}
    runs-on: ${{ matrix.os }}
    steps:
      - name: Normalize Git line endings on Windows
        run: git config --global core.autocrlf input

      - name: Checkout target repository
        uses: actions/checkout@v6
        with:
          path: ${{ github.repository }}

      - name: Checkout amigo repository
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.amigo_repository || 'yongtenglei/Amigo' }}
          path: amigo

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ inputs.go-version }}
          go-version-file: ${{ format('{0}/{1}', github.repository, inputs.go-version-file) }}
          cache: true
          cache-dependency-path: ${{ format('{0}/{1}', github.repository, inputs.go-version-file) }}

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: ${{ inputs.golangci_version || 'v2.11.3' }}
          working-directory: ${{ format('{0}/{1}', github.repository, inputs.directory || '') }}
          args: >
            --config=${{ inputs.golangci_path || format('{0}/amigo/golangci.yml', github.workspace) }}
            --timeout=${{ inputs.timeout || '10m' }}
            ./...
